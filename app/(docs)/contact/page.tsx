import { ContactPage } from '@/features/general/pages/contact-page';

export const metadata = {
  title: 'Contact - JAMMAII',
  description: 'Interested in crowdfunding with JAMMAII ? contact us.'
};

export default function Page() {
  return <ContactPage />;
}
